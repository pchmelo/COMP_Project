class CompileArithmetic{

    public int foo() {
        int a;
                int[] b;

                b = [true, true, true];

                a = this.varargs(b);

        return a + b;
    }

    public int varargs(boolean... a) {
        this.foo();
        return a[0];
    }
}